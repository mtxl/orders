<li><%= context_menu_link l(:label_attachments_bulk_download), {:controller => 'attachments',
                                              :action => 'bulk_download',
                                              :issues_ids => @issues.collect(&:id),
                                              :back_url => @back},
                          :method => :get %>
</li>
